dist: xenial
language: csharp

services:
  - docker
script: 
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker build -t $DOCKER_USERNAME/asprakendus:v1 .
  - docker push $DOCKER_USERNAME/asprakendus:v1
